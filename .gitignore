node_modules/
dist/
# session scratch: recordings, frame dumps, one-off probes
scratchpad/
*.log
.DS_Store
.claude/
# personal dev deploy script
deploy.sh
deploy-dev.sh
deploy.env
# never commit secrets
*.secret
.env

# where the one dev instance runs, when not here (docker/remote.mjs)
docker/.remote
